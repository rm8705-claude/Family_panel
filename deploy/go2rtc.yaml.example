# go2rtc — live camera streams for the Family Panel.
#
# WHY THIS EXISTS
# Home Assistant cannot serve real live video for an RTSP camera. Its Generic
# Camera integration has no MJPEG to hand out, so /api/camera_proxy_stream
# grabs single frames with ffmpeg and staples them together at the
# integration's frame rate — a slideshow, however fast anything asks for it.
# go2rtc holds ONE RTSP session open per channel and serves genuine MJPEG off
# it, which an <img> can read directly. That is what the panel's per-tile
# `stream_url` points at (see family_panel/config.yaml).
#
# WHERE THIS GOES
# Save this file as go2rtc.yaml in the Home Assistant config folder (the same
# folder as configuration.yaml).
#
# The add-on is NOT in the default add-on store — add the repository first:
#   Settings -> Add-ons -> Add-on Store -> (top right) Repositories
#   -> https://github.com/AlexxIT/hassio-addons -> Add
# then refresh the store and install "go2rtc". That repository also ships a
# go2rtc-master-hw build with hardware acceleration, which is worth trying if
# the Pi struggles with the transcode below — it is the master build rather
# than the stable one, so it is the fallback, not the starting point.
#
# Use the add-on, NOT Home Assistant's own bundled go2rtc: the bundled one is
# for HA's internal WebRTC use, listens on 11984 rather than 1984, and is not
# exposed to other clients unless debug_ui is turned on.
#
# BEFORE IT WILL WORK
# Replace YOUR_DVR_PASSWORD (6 places) with the DVR's admin password. That is
# the only thing left to fill in — the channel numbers below are the real
# mapping off the Generic Camera integrations. This file is the example and
# keeps a placeholder; the real one lives on the HA box only, never in git.
#
# PASTE THE TEXT, DO NOT DOWNLOAD THE FILE. Getting this file onto the box as
# a *file* — downloaded on a phone, shared between apps — is how it arrived
# once as a zip archive saved under a .yaml name. go2rtc read it, found no
# YAML, configured no streams, and the panel quietly fell back to the Home
# Assistant stills it was already showing: everything looked installed and
# nothing had changed. If the config tab's first line begins "PK", that is a
# zip signature, not config. Easiest cure is go2rtc's own config editor —
# paste there and Save & Restart, no file handling at all.
#
# MAIN STREAM (/0), ENCODED DOWN TO 1280 WIDE
# The sub stream (/1) was the first cut and looked bad: it is deliberately
# low-resolution, which is fine as a thumbnail and ugly blown up full screen.
# So this reads the main stream for real detail, and #width=1280 caps what
# gets MJPEG-encoded — the panel's full-screen view is about 1100px wide, so
# encoding the full 5 MP would cost plenty and show nothing extra.
#
# Levers if the Pi struggles, in the order worth trying:
#   #width=960            — smaller encode
#   #raw=-r 12            — cap the frame rate (a wall panel does not need 25)
#   #hardware             — hardware encode, if the build supports it
#                           (go2rtc-master-hw in the same add-on repository)
# And if it is sharpness rather than smoothness that disappoints, add
#   #raw=-q:v 3           — better JPEG quality, 2 is near-best, higher is worse
#
# ON-DEMAND
# go2rtc connects to a channel only while something is watching and drops the
# connection when the last viewer leaves, so an idle panel costs nothing. That
# is also why the panel leaves `snapshot_url` unset and keeps the wall's stills
# on Home Assistant — pointing six stills at go2rtc would wake all six streams
# every refresh and throw the saving away.
#
# The stream NAMES here are what the panel asks for by ?src=<name>, so they
# have to keep matching family_panel/config.yaml. The CHANNELS are the DVR's,
# and the two orders are not the same — ch01 is the front door, not the
# carport. Listed in channel order to make it easy to check against the DVR.

streams:
  # ch01
  front_door:
    - rtsp://admin:YOUR_DVR_PASSWORD@192.168.1.2:554/ch01/0
    - ffmpeg:front_door#video=mjpeg#width=1280

  # ch02
  rear_patio:
    - rtsp://admin:YOUR_DVR_PASSWORD@192.168.1.2:554/ch02/0
    - ffmpeg:rear_patio#video=mjpeg#width=1280

  # ch03
  carport:
    - rtsp://admin:YOUR_DVR_PASSWORD@192.168.1.2:554/ch03/0
    - ffmpeg:carport#video=mjpeg#width=1280

  # ch04
  rear_to_front:
    - rtsp://admin:YOUR_DVR_PASSWORD@192.168.1.2:554/ch04/0
    - ffmpeg:rear_to_front#video=mjpeg#width=1280

  # ch05
  front_to_back:
    - rtsp://admin:YOUR_DVR_PASSWORD@192.168.1.2:554/ch05/0
    - ffmpeg:front_to_back#video=mjpeg#width=1280

  # ch06
  train_side:
    - rtsp://admin:YOUR_DVR_PASSWORD@192.168.1.2:554/ch06/0
    - ffmpeg:train_side#video=mjpeg#width=1280

# Each stream lists two sources. The first is the camera itself. The second
# re-encodes that same stream to MJPEG, and is what makes an MJPEG request
# possible at all — go2rtc will only serve a codec something can produce, and
# the DVR sends H.264.

api:
  listen: ":1984"     # what the panel's stream_url talks to

# Check it before touching the panel: open http://192.168.1.21:1984 and play a
# stream from go2rtc's own page. If it plays there and not on the panel, the
# problem is the panel's URL; if it fails there too, it is this file or the
# DVR — go2rtc's log will say which.
